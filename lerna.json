{
    "lerna": "3.13.0",
    "npmClient": "npm",
    "version": "independent",
    "packages": ["src/**/*"],
    "registry": "https://skimdb.npmjs.com/registry",
    "command": {
        "publish": {
            "ignoreChanges": ["*.md", "tests/*", "ignored-files"]
        }
    }
}
