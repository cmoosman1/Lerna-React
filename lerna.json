{
    "lerna": "3.13.0",
    "npmClient": "npm",
    "version": "independent",
    "packages": ["src/**/*"],
    "registry": "http://registry.npmjs.org/",
    "command": {
        "publish": {
            "ignoreChanges": ["*.md", "tests/*", "ignored-files"]
        }
    }
}
